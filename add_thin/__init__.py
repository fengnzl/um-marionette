"""
Project Template
基于项目模板的初始文件
"""

# 定义项目的版本号属性，当前版本为 0.1.0
__version__ = "0.1.0"

"""
========================================================================
【给外行新手的通俗讲解】 -- add_thin/__init__.py
========================================================================

1. 这个文件是什么？
   - 在 Python 语言里，任何包含 `__init__.py` 的文件夹都会被视为一个“包（Package）”，意味着里面的代码可以被其他人引入和调用。
   - 它通常是空的，或者像这里一样仅仅记录一下版本号（__version__）。

2. 它的作用
   - 它是一种“身份证明”，告诉系统 `add_thin` 不只是一个普通存放文件的文件夹，而是一个拥有代码模块层级结构的正规包。这保证了在 `configs.py` 里面的 `from add_thin...` 能够正常运行。
========================================================================
"""
