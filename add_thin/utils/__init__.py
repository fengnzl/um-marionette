# 将各个子文件里公用的工具函数一口气在这里进行批量导出
from .callbacks import *   # 导出关于训练回调（暂停/记录点）相关的代码
from .exceptions import *  # 导出自定义错误类相关的代码
from .logging import *     # 导出日志或屏幕打印相关的代码
from .seed import *        # 导出随机种子设定相关的代码

"""
========================================================================
【给外行新手的通俗讲解】 -- add_thin/utils/__init__.py
========================================================================

1. 为什么这四句话只有 from 和 * 符号？
   - 这在 Python 里面叫“暴露接口”。原本工具代码被分门别类塞在好几个抽屉（不同的`.py`文件）里。
   - 为了方便外面的人拿工具，这个文件把所有的工具一口气倒在一个前台桌面上。
   - 外面写代码的人，只需要调 `from add_thin.utils import get_logger` 就能拿到日志器，不用关心它原本到底是在 `logging.py` 还是在哪里。这极大地简化了项目的引用。
========================================================================
"""
